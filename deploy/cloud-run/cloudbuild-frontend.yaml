steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - build
      - -f
      - frontend/Dockerfile.prebuilt
      - -t
      - $_IMAGE
      - .   # build context = repo root

images:
  - $_IMAGE
